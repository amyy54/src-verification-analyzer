import { ConfigAlert } from "./src/Util"

export const config = {
    path: "",
    staticPath: ""
};

// View src/Util.ts to see the properties of ConfigAlert.
export const alert: ConfigAlert | undefined = undefined;
